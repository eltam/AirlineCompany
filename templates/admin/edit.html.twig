{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-4">
        <h1>Modifier les informations</h1>
        {{ include(path, {form: form, button: 'Editer'}) }}
    </div>
{% endblock %}